# Set these in Vercel → Settings → Environment Variables (Production + Preview)
# Do NOT commit real secrets.

MONGODB_URI=
JWT_SECRET=
JWT_DAYS=365d
MIGHTPULSE_API_KEY=
NODE_ENV=production
CLIENT_URL=https://kingshot-calculator.vercel.app
CORS_ORIGINS=https://kingshot-calculator.vercel.app,http://localhost:3000,http://localhost:5173
ALLOW_VERCEL_PREVIEWS=true

# Leave empty on Vercel full-stack (same origin /api)
# VITE_API_URL=
